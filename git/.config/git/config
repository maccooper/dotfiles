[user]
	name = Mac Cooper
	email = Mackenzie.Cooper99@gmail.com
[pull]
	ff = only
[push]
	autoSetupRemote = true
[url "git@github.com:"]
	insteadOf = "gh:"
[alias]
    lol = log --oneline --graph --decorate
    graph = log --oneline --graph --decorate
    co = checkout
    ci = commit
    st = status
    br = branch
    blame = blame
    amend = commit --amend --no-edit
    unstage = reset HEAD --
    undo = reset --soft HEAD^
    undo-commit = reset --soft HEAD^
[core]
    editor = nvim
    excludesFile = ~/.gitignore

[status]
    branch = true
    showStash = true
    showUntrackedFiles = all
[diff]
    context = 3
    rename = copies
    interHunkContext = 10

[color "diff"]
    meta        = black bold
    frag        = magenta
    context     = white
    whitespace  = yellow reverse
    old         = red

[push]
    autoSetupRemote = true
    default = current
    followTags = true
[pull]
    default = current
    rebase = true

[rebase]
    autoStash = true
    autosquash = true
    preserveMerges = true



[init]
	defaultBranch = main
